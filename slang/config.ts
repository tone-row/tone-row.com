import { SlangConfig } from "@tone-row/slang";

const config: Partial<SlangConfig> = {
  baseFontFamily: "nm",
  baseFontSizePx: 20,
  baseFontLineHeight: 1.3,
  inverseTypeScaleLineHeight: 0.98,
};

export default config;
